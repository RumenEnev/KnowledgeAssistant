# Copy to .env and adjust for your environment before running docker compose.

# Postgres connection string used by the API (pgvector-enabled instance).
KA_DB_CONNECTION_STRING=Host=192.168.0.200;Port=5432;Database=postgres;Username=postgres;Password=changeme

# Base URL of the Ollama server used for chat/embeddings.
KA_OLLAMA_BASE_URL=http://192.168.0.200:11434
