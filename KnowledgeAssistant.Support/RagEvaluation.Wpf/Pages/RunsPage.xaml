<Page
    x:Class="RagEvaluation.Desktop.Pages.RunsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
    Title="Runs"
    d:DesignHeight="700"
    d:DesignWidth="900"
    mc:Ignorable="d">

    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock
            Grid.Row="0"
            Margin="0,0,0,16"
            FontSize="24"
            FontWeight="SemiBold"
            Text="Evaluation Runs" />

        <StackPanel
            Grid.Row="1"
            Margin="0,0,0,12"
            HorizontalAlignment="Left"
            Orientation="Horizontal">
            <ui:Button
                x:Name="RefreshButton"
                Padding="12,6"
                Click="RefreshButton_Click"
                Content="Refresh"
                Icon="{ui:SymbolIcon ArrowSync24}" />
            <ui:Button
                x:Name="CleanDatabaseButton"
                Margin="8,0,0,0"
                Padding="12,6"
                Appearance="Danger"
                Click="CleanDatabaseButton_Click"
                Content="Clean Database"
                Icon="{ui:SymbolIcon Delete24}" />
        </StackPanel>

        <DataGrid
            x:Name="RunsGrid"
            Grid.Row="2"
            AutoGenerateColumns="False"
            IsReadOnly="True"
            MouseDoubleClick="RunsGrid_MouseDoubleClick"
            SelectionMode="Single">
            <DataGrid.Columns>
                <DataGridTextColumn
                    Width="60"
                    Binding="{Binding Id}"
                    Header="Id" />
                <DataGridTextColumn
                    Width="180"
                    Binding="{Binding RunName}"
                    Header="Run Name" />
                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding ChatProvider}"
                    Header="Chat Provider" />
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding ChatModel}"
                    Header="Chat Model" />
                <DataGridTextColumn
                    Width="110"
                    Binding="{Binding JudgeProvider}"
                    Header="Judge Provider" />
                <DataGridTextColumn
                    Width="150"
                    Binding="{Binding JudgeModel}"
                    Header="Judge Model" />
                <DataGridTextColumn
                    Width="160"
                    Binding="{Binding CreatedAt}"
                    Header="Created" />
            </DataGrid.Columns>
        </DataGrid>

        <TextBlock
            x:Name="SummaryText"
            Grid.Row="3"
            Margin="0,16,0,0"
            FontFamily="Consolas"
            Text="Double-click a run to view its summary."
            TextWrapping="Wrap" />
    </Grid>
</Page>
