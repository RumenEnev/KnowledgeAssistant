<Window
    x:Class="KnowledgeAssistant.Wpf.Windows.DocumentsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Manage Documents"
    Width="600"
    Height="760"
    Loaded="DocumentsWindow_Loaded"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView
            x:Name="documentsList"
            Grid.Row="0"
            Margin="0,0,0,8"
            ItemsSource="{Binding Documents}"
            SelectionChanged="DocumentsList_SelectionChanged">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="200"
                        DisplayMemberBinding="{Binding Title}"
                        Header="Title" />
                    <GridViewColumn
                        Width="200"
                        DisplayMemberBinding="{Binding TopicsDisplay}"
                        Header="Topics" />
                    <GridViewColumn Width="100">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Padding="8,2"
                                    Click="DeleteDocument_Click"
                                    Content="Delete"
                                    Tag="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <GroupBox
            Grid.Row="1"
            Margin="0,0,0,8"
            Header="{Binding FormHeader}">
            <Grid Margin="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="120" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock
                    Grid.Row="0"
                    Margin="0,2,0,0"
                    FontWeight="Bold"
                    Text="Title (required)" />
                <TextBox
                    Grid.Row="1"
                    Margin="0,2,0,6"
                    Text="{Binding NewTitle, UpdateSourceTrigger=PropertyChanged}"
                    ToolTip="A short, descriptive name for the document." />

                <TextBlock
                    Grid.Row="2"
                    Margin="0,2,0,0"
                    FontWeight="Bold"
                    Text="Topics (select at least one, required)" />
                <Grid Grid.Row="3" Margin="0,2,0,6">
                    <ToggleButton
                        x:Name="topicsDropdownToggle"
                        Padding="8,5"
                        HorizontalContentAlignment="Left"
                        Content="{Binding SelectedTopicsSummary}"
                        ToolTip="Documents are grouped and searched by topic." />

                    <Popup
                        Width="{Binding ActualWidth, ElementName=topicsDropdownToggle}"
                        AllowsTransparency="True"
                        IsOpen="{Binding IsChecked, ElementName=topicsDropdownToggle}"
                        Placement="Bottom"
                        PlacementTarget="{Binding ElementName=topicsDropdownToggle}"
                        StaysOpen="False">
                        <Border
                            Background="White"
                            BorderBrush="Gray"
                            BorderThickness="1">
                            <TreeView
                                MaxHeight="220"
                                BorderThickness="0"
                                ItemsSource="{Binding TopicTree}">
                                <TreeView.ItemContainerStyle>
                                    <Style TargetType="{x:Type TreeViewItem}">
                                        <Setter Property="IsExpanded" Value="{Binding IsExpanded, Mode=TwoWay}" />
                                    </Style>
                                </TreeView.ItemContainerStyle>
                                <TreeView.ItemTemplate>
                                    <HierarchicalDataTemplate ItemsSource="{Binding Children}">
                                        <CheckBox
                                            Margin="2,3"
                                            Content="{Binding Name}"
                                            IsChecked="{Binding IsSelected, Mode=TwoWay}" />
                                    </HierarchicalDataTemplate>
                                </TreeView.ItemTemplate>
                            </TreeView>
                        </Border>
                    </Popup>
                </Grid>

                <TextBlock
                    Grid.Row="4"
                    Margin="0,2,0,0"
                    FontWeight="Bold"
                    Text="Text (required)" />
                <TextBox
                    Grid.Row="5"
                    Margin="0,2,0,0"
                    AcceptsReturn="True"
                    Text="{Binding NewText, UpdateSourceTrigger=PropertyChanged}"
                    TextWrapping="Wrap"
                    ToolTip="The full document content to ingest and index."
                    VerticalScrollBarVisibility="Auto" />
                <StackPanel
                    Grid.Row="6"
                    Margin="0,4,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Padding="8,4"
                        Click="LoadTextFromFile_Click"
                        Content="Load from file…"
                        ToolTip="Populate the text box with the contents of a .txt file." />
                    <Button
                        Margin="8,0,0,0"
                        Padding="10,4"
                        Click="CancelEdit_Click"
                        Content="Cancel"
                        Visibility="{Binding CancelButtonVisibility}" />
                    <Button
                        Margin="8,0,0,0"
                        Padding="10,4"
                        Click="AddDocument_Click"
                        Content="{Binding SubmitButtonText}"
                        IsEnabled="{Binding CanSubmit}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <GroupBox
            Grid.Row="2"
            Margin="0,0,0,8"
            Header="Retrieval Settings"
            IsEnabled="{Binding IsRetrievalPanelVisible}">
            <StackPanel Margin="4">
                <TextBlock
                    Margin="0,0,0,6"
                    Text="Per-document tuning for chunking and retrieval. Select a document above to edit. Chunk size/overlap apply on next re-ingest; the rest apply immediately to future queries."
                    TextWrapping="Wrap" />
                <UniformGrid Columns="3" Rows="3">
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Chunk size (chars)" />
                        <TextBox Text="{Binding ChunkSize, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Chunk overlap (chars)" />
                        <TextBox Text="{Binding ChunkOverlap, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Candidate pool size" />
                        <TextBox Text="{Binding CandidatePoolSize, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Candidate fanout" />
                        <TextBox Text="{Binding CandidateFanout, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Max distance threshold" />
                        <TextBox Text="{Binding MaxDistanceThreshold, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="RRF k" />
                        <TextBox Text="{Binding RrfK, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Target injection fraction" />
                        <TextBox Text="{Binding TargetInjectionFraction, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                    <StackPanel Margin="0,0,16,8">
                        <TextBlock FontWeight="Bold" Text="Max injection fraction" />
                        <TextBox Text="{Binding MaxInjectionFraction, UpdateSourceTrigger=PropertyChanged}" />
                    </StackPanel>
                </UniformGrid>
                <StackPanel
                    Margin="0,4,0,0"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Padding="10,4"
                        Click="ResetRetrievalConfig_Click"
                        Content="Reset to Default"
                        ToolTip="Removes the saved override; this document falls back to the app default." />
                    <Button
                        Margin="8,0,0,0"
                        Padding="10,4"
                        Click="SaveRetrievalConfig_Click"
                        Content="{Binding SaveRetrievalConfigButtonText}"
                        IsEnabled="{Binding CanSaveRetrievalConfig}" />
                </StackPanel>
            </StackPanel>
        </GroupBox>

        <StatusBar Grid.Row="3">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>