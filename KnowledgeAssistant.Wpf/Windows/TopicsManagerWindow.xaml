<Window
    x:Class="KnowledgeAssistant.Wpf.Windows.TopicsManagerWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    Title="Manage Topics"
    Width="420"
    Height="520"
    Loaded="TopicsManagerWindow_Loaded"
    WindowStartupLocation="CenterOwner">

    <Grid Margin="8">
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <ListView
            x:Name="topicsList"
            Grid.Row="0"
            Margin="0,0,0,8"
            ItemsSource="{Binding Topics}">
            <ListView.View>
                <GridView>
                    <GridViewColumn
                        Width="200"
                        DisplayMemberBinding="{Binding Name}"
                        Header="Name" />
                    <GridViewColumn Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Padding="8,2"
                                    Click="EditTopic_Click"
                                    Content="Rename"
                                    Tag="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                    <GridViewColumn Width="80">
                        <GridViewColumn.CellTemplate>
                            <DataTemplate>
                                <Button
                                    Padding="8,2"
                                    Click="DeleteTopic_Click"
                                    Content="Delete"
                                    Tag="{Binding}" />
                            </DataTemplate>
                        </GridViewColumn.CellTemplate>
                    </GridViewColumn>
                </GridView>
            </ListView.View>
        </ListView>

        <GroupBox
            Grid.Row="1"
            Margin="0,0,0,8"
            Header="{Binding FormHeader}">
            <Grid Margin="4">
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBox
                    Grid.Row="0"
                    Margin="0,2,0,6"
                    Text="{Binding NewTopicName, UpdateSourceTrigger=PropertyChanged}"
                    ToolTip="A short, descriptive topic name." />

                <StackPanel
                    Grid.Row="1"
                    HorizontalAlignment="Right"
                    Orientation="Horizontal">
                    <Button
                        Margin="0,0,8,0"
                        Padding="10,4"
                        Click="CancelEdit_Click"
                        Content="Cancel"
                        Visibility="{Binding CancelButtonVisibility}" />
                    <Button
                        Padding="10,4"
                        Click="SubmitTopic_Click"
                        Content="{Binding SubmitButtonText}"
                        IsEnabled="{Binding CanSubmit}" />
                </StackPanel>
            </Grid>
        </GroupBox>

        <StatusBar Grid.Row="2">
            <StatusBarItem>
                <TextBlock Text="{Binding StatusMessage}" />
            </StatusBarItem>
        </StatusBar>
    </Grid>
</Window>
